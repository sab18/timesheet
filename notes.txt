future ideas/fixes:
[] fix scrolling on page 1
[] make yearly look nicer
[] add plots??
[] allow . instead of 0.

V1.0.0:
FRONT END:
- input tab
    - input hrs to each project (organized by category from projects.yaml)
    - only accepts positive floats
    - calendar that auto selects today
    - location selector from locations.yaml
    - must input details section
    - notification appears if missing data or notifies submitted
- view hrs tab
    - see weekly summary by project code for past 4 weeks
    - displays rounded hrs worked scaled to 8 hrs
    - lists daily totals for each week
    - rows toggle colors
    - data updates (excel data is fetched) when tab is selected
- monthly fte tab
    - see monthly summaries of projects worked, hrs per project, %
- yearly fte tab
    - see yearly summary of projects worked, hrs per project, %
- configure tab
    - select projects.yaml, locations.yaml, and Timesheet.xlsx as desired
    - pages auto refresh after new files are uploaded

BACK END:
- input projects.yaml and locations.yaml files
- projects: project_nickname, project_name, cost_center_name, cost_center_code
- "db" = Timesheet.xlsx file.  digest can be edited as desired
- all files autogenerate when initialze.py is run
